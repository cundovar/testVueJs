export default class Bloc{
    constructor(title,text,buttons,img,colorText){

        this.title=title
        this.text=text
        this.buttons=buttons
        this.img=img
        this.colorText=colorText
    }
}